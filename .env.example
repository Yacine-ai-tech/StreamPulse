# StreamPulse — Real-time data pipeline
POSTGRES_URL=
LLM_DEFAULT=your-preferred-model
LLM_JUDGE=your-preferred-model-for-judging

API_KEY=

WEBHOOK_SECRET=change_me

# E2E test admin login password, must match deployed admin account
ADMIN_PASS=
# Shared secret for internal service-to-service auth, must match the gateway and all other OmniIntel services — also referenced by the n8n workflow JSON configs under connectors/n8n/workflows/ via {{$env.OMNIINTEL_INTERNAL_TOKEN}}, so it must also be set as an environment variable inside your n8n instance
OMNIINTEL_INTERNAL_TOKEN=

DOCINTEL_URL=https://docintel-f4g1.onrender.com

LOG_LEVEL=INFO
CORS_ALLOWED_ORIGINS=https://intelai.ysiddo-ai-projects.app,https://frontend-brown-five-90.vercel.app
TELEMETRY_OPT_OUT=true

# n8n & External Credentials (Auto-provisioned)
N8N_BASE_URL=
N8N_API_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
CLICKUP_API_KEY=
CLICKUP_WORKSPACE_ID=
